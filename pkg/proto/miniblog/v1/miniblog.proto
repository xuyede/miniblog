// Copyright 2026 Deye <xuyede77@gmail.com>. All rights reserved.
// Use of this source code is governed by a MIT style
// license that can be found in the LICENSE file. The original repo for
// this file is https://github.com/xuyede/miniblog.

syntax = "proto3";

package v1;

import "google/protobuf/timestamp.proto";

option go_package = "github.com/xuyede/miniblog/pkg/proto/miniblog/v1";

// miniblog 定义一个 miniblog RPC 服务
service MiniBlog {
  rpc ListUser(ListUserRequest) returns (ListUserResponse) {}
}

// ListUserRequest 指定了 `ListUser` 接口的请求参数.
// optional 表示该字段是可选的
message ListUserRequest {
  optional int64 limit  = 1;
  optional int64 offset = 2;
}

message UserInfo {
  string                    username  = 1;
  string                    nickname  = 2;
  string                    email     = 4;
  string                    phone     = 5;
  int64                     postCount = 6;
  google.protobuf.Timestamp createdAt = 7;
  google.protobuf.Timestamp updatedAt = 8;
}

// ListUserResponse 指定了 `GET /v1/users` 接口的返回参数，相当于 HTTP Response.
// repeated 表示该字段是一个数组
message ListUserResponse {
  int64             totalCount = 1;
  repeated UserInfo Users      = 2;
}